{
  "name": "napi-oop-example-native-add",
  "version": "0.1.0",
  "private": true,
  "description": "Example: same #[napi] source built in-proc as a real native .node addon",
  "main": "index.js",
  "types": "index.d.ts",
  "scripts": {
    "build:native": "napi build --platform --release",
    "build:ts": "tsc",
    "build": "npm run build:native && npm run build:ts",
    "start": "node dist/main.js"
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.16.5",
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
