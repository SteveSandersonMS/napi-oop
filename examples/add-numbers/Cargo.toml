[package]
name = "add-numbers-example"
version.workspace = true
edition.workspace = true
publish = false

[[bin]]
name = "add-numbers-provider"
path = "src/main.rs"

[dependencies]
napi-oop = { path = "../../crates/napi-oop" }
napi-oop-macro = { path = "../../crates/napi-oop-macro", features = ["out-of-proc"] }
