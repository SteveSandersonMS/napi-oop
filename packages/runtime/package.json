{
  "name": "napi-oop-runtime",
  "version": "0.1.0",
  "description": "Node-side runtime for napi-oop: connects to an out-of-process Rust peer.",
  "license": "MIT OR Apache-2.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "napi-oop-codegen": "dist/codegen-cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "msgpackr": "^2.0.4"
  }
}
