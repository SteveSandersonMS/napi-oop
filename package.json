{
  "name": "napi-oop-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Out-of-process napi: run #[napi] Rust in a separate process from Node.",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "devDependencies": {
    "@napi-rs/cli": "^2.18.4"
  }
}
